class Expense:
        def __init__(self, amount, data, category, description):
                self.amount = amount
                self.data = data
                self.category = category
                self.description = description
